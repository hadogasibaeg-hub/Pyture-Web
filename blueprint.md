# Pyture Search Engine

## Overview

Pyture is a web-based search engine with a user interface inspired by Google. This document outlines the project's features, design, and implementation details.

## Current State

### Implemented Features
- **Project Initialization:** The project is set up with basic `index.html`, `style.css`, and `main.js` files.

### Design and Style
- **Initial Design:** A simple "Hello World" page.

## Current Task: Build the basic search interface

### Plan
1.  **Create `blueprint.md`:** Establish a single source of truth for the project's vision and progress.
2.  **Modify `index.html`:**
    *   Change the page title to "Pyture".
    *   Add a header for the "Pyture" logo.
    *   Create a form for the search bar and search button.
3.  **Modify `style.css`:**
    *   Apply a clean, modern design reminiscent of Google.
    *   Center the search components on the page.
    *   Style the search input and button.
4.  **Modify `main.js`:**
    *   Add an event listener to the search form to capture the search query.
    *   For now, log the query to the console when a search is performed.
